---
import MainLayout from "../../../layouts/MainLayout.astro";
import EquipmentDataJsonFile from "../../../../public/data/CubicEntityDataJsonFile.json";
//
export async function getStaticPaths() {
	return EquipmentDataJsonFile.equipmentCubicDataJsonArray.map((entry) => ({
		params: {
			equipmentName: entry.Identifier.toLowerCase().split(" ").join("-"),
		},
		props: { equipment: entry },
	}));
}
//
const { equipment } = Astro.props;
---

<MainLayout topTitle={equipment.Identifier} />
